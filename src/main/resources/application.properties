spring.application.name=OpticalShop-backend

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/ecommerce?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

stripe.api.key=${STRIPE_API_KEY}
jwt.secret=${JWT_SECRET:mysecretkeymysecretkeymysecretkeymysecretkey}
app.security.password.regex=^(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z])(?=.*[@#$%^&+=!]).{8,}$
app.url=http://localhost:8080
cors.allowed.origins=${cors.allowed.origins:http://localhost:3000,http://localhost:8080}